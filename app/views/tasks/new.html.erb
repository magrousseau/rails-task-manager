<h1>New task</h1>

<%= simple_form_for(@task) do |f| %>
<%= f.input :title %>
<%= f.input :details %>
<%= f.submit "add task", class: 'btn btn-primary' %>
<%end %>

<%= link_to "<- back to list", tasks_path %>
